"""Concrete file, platform and persistence adapters.

The root ``config`` directory stores user-editable configuration data.
This package contains only Python code that reads, writes or locates external
application resources.
"""
